statechart TestAutWithCharExprCorrect {

  state B;
  initial state A;
  state C;
  state D;
  state E;

  A -> B ['b'] / {PRINT};
  B -> C ['c'] / {EXEC};
  C -> D ['d'] / {EXEC};
  C -> E ['e'] / {EXEC};
}

